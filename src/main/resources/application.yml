spring:
  application:
    name: cash-manager-backend
  profiles:
    active: default
  datasource:
    url: ${DATABASE_URL}
    password: ${DATABASE_PASSWORD}
    username: ${DATABASE_USERNAME}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  mail:
    username: ${EMAIL}
    password: ${EMAIL_PASSWORD}
    host: smtp.gmail.com
    port: 465
    protocol: smtps
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  messages:
    basename: i18n/messages
    fallback-to-system-locale: false

springdoc:
  api-docs:
    path: /api/v1/api-docs
  swagger-ui:
    path: /api/v1/swagger-ui
    disable-swagger-default-url: true

cashmanager:
  security:
    jwt:
      issuer: ${JWT_ISSUER}
      access-token-minutes-amount: ${JWT_ACCESS_TOKEN_MINUTES_AMOUNT}
      refresh-token-minutes-amount: ${JWT_REFRESH_TOKEN_MINUTES_AMOUNT}
    rsa:
      access-public-key: ${ACCESS_PUBLIC_KEY}
      access-private-key: ${ACCESS_PRIVATE_KEY}
      refresh-public-key: ${REFRESH_PUBLIC_KEY}
      refresh-private-key: ${REFRESH_PRIVATE_KEY}

server:
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never

logging:
  level:
    org:
      springframework: INFO